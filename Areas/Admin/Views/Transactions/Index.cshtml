@model PagedList.IPagedList<BTL_LTWeb.Models.EF.Transaction>
@using PagedList.Mvc;
@using PagedList;

@{
    ViewBag.Title = "Transaction";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
<div class="card-header"><h3 class="text-center font-weight-light my-4">Transaction list</h3></div>

@*<form class="d-none d-md-inline-block form-inline ms-auto me-0 me-md-3 my-2 my-md-0">*@
@using (Html.BeginForm("Index", "Transactions", FormMethod.Get, new { @class = "d-none d-md-inline-block form-inline ms-auto me-0 me-md-3 my-2 my-md-0" }))
{
    <div class="input-group" style="margin-top: 20px">
        <input class="form-control" type="text" name="searchString" placeholder="Search for..." aria-label="Search for..." aria-describedby="btnNavbarSearch" value="@ViewBag.SearchString" />
        <button class="btn btn-primary" id="btnNavbarSearch" type="submit"><i class="fas fa-search"></i></button>
    </div>
}
@*</form>*@

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">

            <!-- /.panel-heading -->
            <div class="panel-body">
                <div class="table-responsive">
                    <table class="table table-striped table-bordered table-hover">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>User ID</th>
                                <th>Name</th>
                                <th>Email</th>
                                <th>Amount</th>
                                <th>Payment</th>
                                <th>Payment details</th>
                                <th>Message</th>
                                <th>Status</th>
                                <th>Security</th>
                                <th>Time created</th>
                                <th>#</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.id</td>
                                    <td>@item.user_id</td>
                                    <td>@item.user_name</td>
                                    <td>@item.user_email</td>
                                    <td>@item.amount</td>
                                    <td>@item.payment</td>
                                    <td>@item.payment_info</td>
                                    <td>@item.message</td>
                                    <td>@item.status</td>
                                    <td>@item.security</td>
                                    <td>@item.time_created</td>
                                    <td>
                                        <a href="/Admin/Transactions/Edit/@item.id">Edit</a>
                                        |
                                        <a href="/Admin/Transactions/Delete/@item.id">Delete</a>
                                    </td>

                                </tr>
                            }
                        </tbody>
                    </table>
                    @Html.PagedListPager(Model, page => Url.Action("Index", new { page, @searchString = ViewBag.SearchString }))
                </div>
                <!-- /.table-responsive -->
            </div>
        </div>
    </div>
</div>



